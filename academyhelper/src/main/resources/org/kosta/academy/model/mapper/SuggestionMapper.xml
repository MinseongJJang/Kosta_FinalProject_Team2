<?xml version="1.0" encoding="UTF-8"?>
<!-- Sql Mapper -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.kosta.academy.model.mapper.SuggestionMapper">
	<insert id="registerSuggestionPost" parameterType="suggestionPostVO">
		insert into suggestion_post(sug_no,sug_title,sug_content,sug_regdate,usr_id)
		values(suggestion_post_seq.nextval,#{sugTitle},#{sugContent},SYSDATE,#{userVO.usrId})
	</insert>
	
	<select id="listSuggestionPost" parameterType="string" resultMap="userInfoRM">
	SELECT sug_no,sug_title,sug_content,sug_regdate,usr_id
		FROM(
		SELECT row_number() over(order by sug_no desc) rnum,sug_no,sug_title,sug_content,sug_regdate,usr_id
		 FROM suggestion_post
		) s where rnum between #{startRowNumber} and #{endRowNumber}
		order by sug_no desc
		
		select sug_no,sug_title,sug_content,sug_regdate,usr_id
		 from(
		select sug_no,sug_title,sug_content,sug_regdate,usr_id
		from suggestion_post) s, where u.usr_id = #{value}
	</select>
</mapper>