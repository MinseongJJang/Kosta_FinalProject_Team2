<?xml version="1.0" encoding="UTF-8"?>
<!-- Sql Mapper -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.kosta.academy.model.mapper.AcademyMapper">
	<insert id="registerAcademy" parameterType="academyVO">
		insert into academy(aca_no,aca_name,aca_addr,aca_tel,usr_id) values(academy_seq.nextval, #{acaName}, #{acaAddr}, #{acaTel}, #{userVO.usrId})
	</insert>
	<select id="listAcademy" resultType="academyVO">
		select aca_no, aca_name, aca_addr, aca_tel from academy
	</select>
	<select id="getTotalListAcaCount" resultType="int">
		select count(*) from academy
	</select>
</mapper>