<?xml version="1.0" encoding="UTF-8"?>
<!-- Sql Mapper -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.kosta.academy.model.mapper.CurriculumMapper">
<insert id="registerCurriculum" parameterType="curriculumVO">
		<selectKey keyProperty="cur_no" resultType="int" order="BEFORE">
			select
			curriculum_seq.nextval from dual
		</selectKey>
		insert into curriculum(cur_no,aca_no,cur_name,limit_mem,cur_content,cur_lecturer,cur_textbook)
		values(#{cur_no},#{aca_no},#{cur_name},#{limit_mem},#{cur_content},#{cur_lecturer},#{cur_textbook})
	</insert>
	<select id="getTotalCurriculumCount" resultType="int">
		select count(*) from curriculum
	</select>
 	<resultMap type="curriculumVO" id="curriculumRM">
		<result property="academyVO.acaNo" column="acaNo" />
	</resultMap> 
	<select id="listCurriculum" resultMap="curriculumRM" parameterType="pagingBean">
		SELECT c.cur_no,c.cur_name,c.limit_mem,c.cur_content,c.cur_lecturer,c.cur_textbook,c.aca_no FROM(
		SELECT row_number() over(order by cur_no desc) as rnum,cur_no,cur_name,limit_mem,cur_content,cur_lecturer,cur_textbook,aca_no
		FROM curriculum
		) c,academy a where c.aca_no=a.aca_no and rnum between #{startRowNumber} and #{endRowNumber}
		order by cur_no desc
	</select>
</mapper>